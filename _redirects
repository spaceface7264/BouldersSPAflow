# SPA: keep /reset-password in the URL. A 200 rewrite to /index.html gets
# canonicalized to / by Pages (308), which drops the route and shows step 1.
/reset-password    /reset-password/   308
/resetPassword     /resetPassword/    308
/reset-password/   /index.html        200
/resetPassword/    /index.html        200

# Redirect /hero to /hero/ so hero landing page loads
/hero    /hero/    302
/firstmonthfree    /firstmonthfree/    302

# /fitness: keep the path. A 200 rewrite of /fitness → /index.html is
# canonicalized to / by Pages, which would drop the landing route.
/fitness    /fitness/    302
/fitness/   /index.html  200
